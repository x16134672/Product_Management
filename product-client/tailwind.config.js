/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Where Tailwind looks for classes
  theme: {
    extend: {}, // Add custom theme extensions here if needed
  },
  plugins: [], // Add Tailwind plugins here if needed
};